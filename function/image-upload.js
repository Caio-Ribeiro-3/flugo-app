async function onRequestGet(context) {
    const response = await context.next();
    return response
}

async function onRequestGet(context) {
    const response = await context.next();
    return response
}